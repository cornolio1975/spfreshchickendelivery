import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.spfreshchicken.delivery',
  appName: 'SP Fresh Chicken Delivery',
  webDir: 'out'
};

export default config;
